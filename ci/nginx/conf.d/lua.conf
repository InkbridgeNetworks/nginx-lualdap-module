    lua_package_cpath "$prefix/modules/?.so;;";

    init_by_lua_block {
        -- Per-worker cache of lualdap connections keyed by cosocket fd,
        -- the same table dapman keeps in ldap.fd_map. Used by
        -- api.d/ldap-pooled.conf.
        lualdap_pooled_fd_map = {}
    }
